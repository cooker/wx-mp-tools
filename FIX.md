# 可复用提示词：精选软件库首页（分类筛选 + 顶栏菜单 + 卡片列表）

> **用途**：把下列章节整体或按需复制给 AI / 协作者，即可在**任意前端项目**中复刻同一套**逻辑与数据流**（不涉及样式、类名、具体 UI 库版本）。  
> **使用前**：先填写「变量表」中的占位符，再执行「实现任务」与「验收标准」。

---

## 0. 变量表（请先替换）

复制到任务描述顶部，填好再开始实现：

```text
[FRAMEWORK]     = 例如 Vue 3
[ROUTER]        = 例如 vue-router
[STATE]         = 例如 Pinia / composable / useState / Zustand（可选，首页本地状态亦可）
[ITEM_TYPE]     = 列表项实体名称，例如 SoftwareItem、Product、Resource
[LIST_SOURCE]   = 唯一数据源：静态数组 / REST API / GraphQL，例如 fetchSoftwareList()
[CATEGORY_ENUM] = 分类联合类型：含「全部」+ N 个具体分类
[ROUTE_HOME]    = 首页路径，例如 /
[ROUTE_DETAIL]  = 详情路径模式，例如 /item/:slug 或 /software/[slug]
[ROUTE_SEARCH]  = 搜索页路径，例如 /search
[THEME_STORAGE] = 主题 localStorage 键名，例如 theme
[THEME_CLASS]   = 根节点深色标记方式，例如 html.dark（与所用 CSS 方案一致）
[NAV_STICKY_VAR]= 可选：顶栏高度 CSS 变量名，例如 --navbar-height（若下方有 sticky 子栏需要贴顶栏）
[DIALOG_PRIMITIVE] = 无障碍弹层：例如 Radix Dialog / Reka UI Dialog / Headless UI Dialog / 自研 focus trap
```

---

## 1. 角色与目标（提示词开头可粘贴）

```text
你是资深前端工程师。请在 [FRAMEWORK] 项目中实现一个「精选库」首页模块，包含：
（1）顶栏全局菜单：主导航、可选主题切换、搜索入口；
（2）可选 Hero 区：与列表数据解耦的标题与说明；
（3）横向单选分类筛选条，选项旁展示该分类下的条目数量；
（4）响应式网格中的「卡片」列表：每条数据一张卡片，支持进入详情与主操作（如下载弹层）。

约束：
- 不写具体样式、不写类名；仅说明布局意图时可用语义（如「顶栏 sticky」「分类条在顶栏下方」）。
- 列表数据必须单一事实来源：过滤结果只能由「当前选中的分类 + [LIST_SOURCE]」派生，禁止再维护一份与过滤结果平行的「当前列表」状态（除非来自服务端分页且语义不同）。
- 分类数量角标必须与当前 [LIST_SOURCE] 一致，通过聚合计算得出，禁止手写与列表不同步的数字。
- 主导航需对当前路径提供明确的「当前项」可访问性语义（例如 aria-current="page" 或框架等价物）。
若 [ROUTER] 支持将路由参数映射为页面 props，详情页推荐用路由参数在 [LIST_SOURCE] 中查找单条；否则用路由钩子读取参数再查找。
```

---

## 2. 信息架构与数据模型（规格）

### 2.1 页面块（自上而下）

1. **应用壳**：全局顶栏 + 主内容出口（路由视图）+ 可选页脚。  
2. **顶栏菜单**：品牌/首页链接、若干主导航项、主题切换（若需要）、搜索入口。  
3. **精选库首页主内容**：Hero（可选）→ 分类筛选条 → 卡片网格。  
4. **卡片**：单条 `[ITEM_TYPE]` 的摘要区、主操作区、次要入口（如「详情」）。

### 2.2 建议的 `[ITEM_TYPE]` 最小字段（可按业务增减）

| 字段 | 用途 |
|------|------|
| `slug` | 详情路由唯一标识 |
| `name` | 标题与搜索 |
| `excerpt` | 摘要与搜索 |
| `category` | 过滤与展示标签（类型为排除「全部」后的具体分类） |
| `version` 或等价 | 版本/更新时间等元信息 |
| `featured` | 可选；是否「精选/推荐」及是否走强调式卡片逻辑 |
| `primaryActionVariant` 或等价 | 可选；主按钮语义分支（如品牌主色 / 中性），仅逻辑分支，不限定样式 |
| `officialUrl` 或等价 | 可选；主操作弹层中的外链 |

### 2.3 分类与过滤规则

- 存在一个 **`currentCategory`** 状态，类型为 `[CATEGORY_ENUM]`。  
- **`currentCategory === '全部'`**（或你定义的「不过滤」枚举值）→ 网格展示全量（或当前页内的全量）条目。  
- 否则 → **`item.category === currentCategory`**。  
- 分类条与 **`currentCategory`** 双向同步（父级为唯一持有者，或经状态管理向上提升，但**不得**在子组件内私自复制一份长期分叉的分类状态）。

### 2.4 分类数量

- 定义函数或 memo：**对 [LIST_SOURCE] 按 `category` 聚合计数**；「全部」= 列表长度（若分页则 = 当前已加载条数或服务端返回 total，二选一并在文档中写清）。  
- 分类条渲染：**选项文案 + 对应计数**，禁止与列表手工对表。

---

## 3. 顶栏菜单（实现任务清单）

- [ ] 主导航每一项映射到确定路径；当前 URL 与该项匹配时标记**当前页**（`aria-current="page"` 或框架推荐写法）。  
- [ ] 搜索入口跳转至 `[ROUTE_SEARCH]`（或打开搜索弹层，但需在变量表中声明为「弹层模式」并等价实现「可聚焦的搜索输入」）。  
- [ ] **主题（若需要）**：读取 `[THEME_STORAGE]`；合法值为显式 light/dark；未设置时跟随 `prefers-color-scheme`；切换时写回存储并在 `[THEME_CLASS]` 目标节点上切换深色类/属性。  
- [ ] **可选：首屏防闪**：在 HTML 入口用与运行时**同一套**读写规则执行一次初始化，再交给框架 hydrate。  
- [ ] **可选：sticky 联动**：若分类条需紧贴顶栏，对顶栏根节点做高度测量（如 `ResizeObserver`），写入 `[NAV_STICKY_VAR]`；子 sticky 的 `top` 依赖该变量（具体单位由实现方决定，本提示词不规定像素）。

---

## 4. 精选软件库首页（实现任务清单）

- [ ] 页面组件只负责：**持有 `currentCategory`**、组合 Hero、挂载分类条与网格。  
- [ ] Hero 文案与配置**不依赖**列表数据（避免改数据牵动营销文案）。  
- [ ] 分类条：`currentCategory` 与父级双向绑定；内部可用单选组、ToggleGroup、Tabs 等，但必须满足**键盘操作与单选语义**（二选一：原生 `radiogroup` 或无障碍组件库）。  
- [ ] 网格：接收 `currentCategory`，**仅通过派生计算**得到 `filteredItems`，再 `map` 渲染卡片。

---

## 5. 卡片（实现任务清单）

- [ ] 卡片为**纯展示 + 明确交互边界**：接收一条 `filteredItems[i]`（或等价 props）。  
- [ ] **进入详情**：摘要区（或整块可点区域）导航到 `[ROUTE_DETAIL]`，用 `slug` 替换路径参数。  
- [ ] **主操作（如下载）**：使用 `[DIALOG_PRIMITIVE]` 实现受控弹层；打开状态为卡片级局部 state 即可。弹层内：若有 `officialUrl` 则展示外链；提供「进入详情」与「关闭」路径；预留多下载源时仍由**同一条 item 或关联 id** 拉取数据。  
- [ ] **次要入口**：与详情同路径的独立按钮/链接，避免与主操作抢同一焦点顺序时，需保证 Tab 顺序合理。  
- [ ] **可选**：向卡片传入 `index`，仅用于 stagger 动画等与数据无关的展示逻辑，不得反写数据源。

---

## 6. 路由与搜索衔接（实现任务清单）

- [ ] **详情页**：用路由参数中的 slug（或 id）在 [LIST_SOURCE] 中 **`find`**；未找到则渲染空状态，不抛未处理异常。  
- [ ] **搜索页**：本地查询字符串对列表（或搜索接口）过滤；命中项链接到与卡片相同的 `[ROUTE_DETAIL]`。  
- [ ] **单一事实来源**：详情与列表若需同步更新，通过刷新 [LIST_SOURCE] 或失效缓存统一更新，避免详情页再维护一份长期分叉的完整列表（除非业务明确要求离线缓存）。

---

## 7. 验收标准（交付前自检）

- [ ] 切换分类后，网格条数与分类角标数量一致。  
- [ ] 无任何路径依赖「手写死」的列表长度或分类计数。  
- [ ] 刷新页面后，主题与（若实现的）`currentCategory` 行为符合变量表约定（若需持久化分类，须在变量表中增加键名并在此验收）。  
- [ ] 顶栏当前项可被读屏识别为当前页。  
- [ ] 弹层打开时焦点管理正确、关闭后焦点回到合理元素（由 `[DIALOG_PRIMITIVE]` 保证或手写补全）。

---

## 8. 反面模式（明确禁止）

- 禁止：`filteredList` 与 `currentCategory` 双写且仅部分同步。  
- 禁止：分类角标写死常量，与真实列表不一致。  
- 禁止：在卡片内 `fetch` 全量列表只为取一条（除非详情独立接口且列表不持有详情）。  
- 禁止：主操作弹层无标题/无描述/无关闭途径（无法满足基础无障碍时须换用 primitive）。

---

## 9. 与参考实现的映射（可选查阅）

以下文件名来自参考仓库 **chunjing-clone**，**非必须**；其他项目请按本提示词的变量表自行命名。

| 概念 | 参考文件（仅供对照） |
|------|----------------------|
| 应用壳 | `App.vue` |
| 顶栏 | `components/layout/AppNav.vue` |
| 首页组合 | `views/HomeView.vue` |
| 分类条 | `components/home/CategoryFilterBar.vue` |
| 网格 | `components/home/SoftwareGrid.vue` |
| 卡片 | `components/home/SoftwareCard.vue` |
| 数据与聚合 | `data/software.ts` |
| 主题 | `composables/useTheme.ts` |
| 顶栏高度 | `composables/useNavbarHeight.ts` |

将本节整块删除后，本文即可作为**零项目绑定**的纯提示词使用。
